
## 紧急（恶性bug，将导致程序崩溃）

## 一级（严重bug）


## 二级（一般bug）

3. 禁用select但无法在其上正常显示title属性(tooltip)
6. 排序存在问题，没有按数字大小排序 -	增加拼音字段

> 暂时处理
- 4. 标题中无法使用1.2...，否则失去标题样式变为单纯的列表样式 => 不要有空格


## 三级（待增重要功能）
0. 隐藏oss路径
1. **草稿**
	- /write在输入标题后自动返回/p/id
2. 编辑器部分：
  - 标题不会自动加粗
  - 用户可以决定是否禁用md格式（为了标题可以写1.2.）
  - **文章长度限制，否则MySQL会报**
  - 下划线和分割线md支持
	- 输入光标距离视口下缘一段距离即开始滚动
	- *长文章懒加载/分段加载*
3. *目录大纲*
	- 内容滚动到对应位置，目录也滚动到对应位置，并强调（加粗、高亮或变色）相应标题
	- 目录可开启关闭
	- 目录项可折叠
4. **上传管理**
	- 用户可以管理上传过的首页背景图/文章封面图
	- 上传时提供几张可选的封面方便用户免于自己找（历史封面）
	- 阿里云OSS如何避免上传重复的
5. 个性化
	- 主题颜色更换
	- 样式可通过设置进行配置
6. **新功能**
	- 评论
	- 自定义标签（给文章分类）
7. 其它
	- **【附件管理页】批量删除**
	- **搜索框搜索（考虑使用搜索引擎）**
	- **游客点击点赞等按钮，弹出登陆弹窗而不是直接403**
	- **文章列表页 侧栏应该增加什么内容**
	- **文章列表页筛选、排序**
	- **用户状态-禁用/未激活**


9. 无限滚动列表也添加页数，点击跳到指定页
	- 采用响应式设计，电脑用页数布局，手机用无限滚动布局
10. 链接仿造知乎那样，可以变为卡片显示


** 表格排序，参照 antdhttps://ant-design.antgroup.com/components/table-cn#table-demo-ajax

## 四级（待增一般功能）
1. 标题也可以修改字体等样式
2. 增加字体
3. 文章判空是否应该包含空格？
4. html标签名语义化 div -> nav/header/article/section...
5. Header的菜单项可以拖动随意调换顺序，支持编辑

1. 自增列（文章唯一索引）不是每次加1，改为其它方式
2. 增加更新日志页面记录历史

1. 【附件管理页】批量删除


## 已归档
[~]1. 对列表中的一行放大缩小字号（编号1001，暂止），
	- 序号数字会有两个/
	- 圆点不再垂直居中
[√]2. 输入不存在的id也能打开文章展示页面
2. 输入汉字时，只打出一个音节而没有输入汉字，占位符依旧存在，不像<input>那样会立即去掉占位符


权限：
- 公开、私有以及密码保护三种模式。
公开权限意味着博客文章可以被任何人访问和阅读，
私有权限则将所有内容设置为仅博主可见，这在博客搭建初期、内容创作阶段或者进行隐私测试时非常有用。
密码保护权限则是介于公开和私有之间的一种选择，博主可以设定一个或多个密码，只有输入正确密码的读者才能访问特定内容
- 为某些关键操作（如删除文章、修改权限等）设置双重认证

性能优化： react-scan